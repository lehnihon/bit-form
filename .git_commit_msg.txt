refactor(core): reduzir surface concreta do BitStore

Remove metodos bridge publicos que existiam apenas como wrappers internos
(emitBeforeValidate, emitAfterValidate, emitBeforeSubmit, emitAfterSubmit,
emitFieldChange, emitOperationalError, updateDependencies, isFieldHidden,
evaluateAllDependencies, getHiddenFields, getRequiredErrors,
clearFieldValidation, handleFieldAsyncValidation, cancelAllValidations,
validateNow, updateDirtyForPath, rebuildDirtyState, clearDirtyState,
buildDirtyValues, batchStateUpdates, resetHistory, getInitialValues,
setInitialValues).

Renomeia metodos como privados:
- internalSaveSnapshot -> saveHistorySnapshot (private)
- batchStateUpdates -> runStateBatch (private)
- dispatch -> dispatch (private)

Extrai BitStoreCapabilityPorts<T> para o bootstrap: cria portas
explicitas com closures em vez de receber a instancia concreta do BitStore.

Privatiza getComputedEntries() e getTransformEntries().

API publica exportada (BitStoreApi, BitStoreHooksApi) nao alterada.
330 testes passando, release gate OK, build typecheck limpo.
