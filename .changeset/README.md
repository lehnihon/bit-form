# Changesets

Use this folder to track semver intent for every relevant change.

## Commands

- Create changeset: `npm run changeset`
- Check status: `npm run changeset:status`
- Apply versioning: `npm run changeset:version`

For PRs that touch runtime behavior, add one changeset file (`.md`).
